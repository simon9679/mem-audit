__pycache__/
*.pyc
.venv/
*.egg-info/
.env

# Run artifacts — never commit. These are produced by the CLI and the
# dev-scripts (confidence test, smoke tests) and are reproducible on demand.
report.json
confidence_report*.json
mem0_config.json
confidence_config.json
confidence_ground_truth.json
*_qdrant_db/
